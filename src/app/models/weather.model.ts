export class Weather {
  constructor(public cityName: string, public description: string, public temperature: number) {}
  // constructor(public location: string, public temperature: number, public todaysForecast: string, public feelsLike: number, public high: number, public low: number, public uvIndex: number, public wind: string, public humidity: number, public dewPoint: number, public pressure: number, public visibility: number) { }
}
